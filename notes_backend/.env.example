# Notes backend environment variables
# Copy to .env and adjust as needed.

# Port/host
PORT=3001
HOST=0.0.0.0
NODE_ENV=development

# Optional: override SQLite DB file path.
# Default (monorepo dev): ../database/myapp.db (resolved automatically)
NOTES_DB_PATH=/absolute/path/to/myapp.db
